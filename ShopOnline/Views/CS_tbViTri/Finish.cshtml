@model Models.CS_tbViTriViewModel

@{
    ViewBag.Title = "Finish";
    Layout = "~/Views/Shared/_Layout_Temp.cshtml";
}
<div class="row">
    <div class="col-lg-12">
        <div class="panel-body">
            <div class="row">
                <div class="col-lg-6">
                    @{
                        if (Model == null)
                        {
                            using (Html.BeginForm("Delete", "CS_tbPhong_Ban", FormMethod.Post))
                            {
                                <p>
                                    <button type="submit" onclick="window.close();" value="Thêm" class="btn btn-success"> <b>ĐÃ XÓA THÀNH CÔNG</b> </button>
                                </p>
                            }
                        }
                        else
                        {
                            using (Html.BeginForm("Delete", "CS_tbPhong_Ban", FormMethod.Post))
                            {
                                <p>
                                    <button type="submit" onclick="window.close();" value="Thêm" class="btn btn-success"> <b>ĐÃ XÓA THÀNH CÔNG</b> </button>
                                </p>
                            }
                        }
                    }
                </div>
                <!-- /.col-lg-6 (nested) -->
            </div>
            <!-- /.row (nested) -->
        </div>
        <!-- /.panel-body -->
    </div>
    <!-- /.col-lg-12 -->
</div>
